

# I may not want it anytime. let it for now
FIND_PACKAGE(FLEX REQUIRED)
FIND_PACKAGE(BISON REQUIRED)

IF(FLEX_FOUND)
	FLEX_TARGET(Scanner pie.ll ${CMAKE_CURRENT_SOURCE_DIR}/lex.yy.cpp)
	BISON_TARGET(Parser pie.y ${CMAKE_CURRENT_SOURCE_DIR}/pie.tab.cpp)
ENDIF(FLEX_FOUND)

AUX_SOURCE_DIRECTORY("${CMAKE_CURRENT_SOURCE_DIR}" SOURCES)

list(APPEND SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/lex.yy.cpp)
list(APPEND SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/pie.tab.cpp)

add_library(pie_compiler STATIC ${SOURCES})
